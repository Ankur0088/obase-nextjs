import styled from "styled-components";
import { FLEX } from "../common/common.style";

export const metricTemplateContainer = styled.div`
  flex: 1;
  padding: 10px;
`;
export const metricTemplateBody = styled.div`
  ${FLEX}
`;
